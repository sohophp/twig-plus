{
  "name": "@twig-plus/formatter",
  "version": "1.0.0",
  "private": true,
  "description": "Reusable Twig formatter core for TwigPlus.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "dependencies": {
    "@twig-plus/parser": "file:../parser"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run test"
  }
}
