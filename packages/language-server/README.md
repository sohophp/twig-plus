# TwigPlus Language Server

This package is a reserved skeleton for a future TwigPlus language server.

It intentionally does not implement LSP behavior yet. When that work starts, it will reuse:

- `@twig-plus/parser`
- `@twig-plus/formatter`
